#GC-Stats-StatusEffects-Controls {
    flex-direction: row;
    flex-wrap: nowrap;
    overflow: hidden;
    margin: var(--gc-common-space-smaller) 0;
}

#GC-Stats-StatusEffects-Controls Button {
    padding: var(--gc-common-space-smaller) var(--gc-common-space-small);
}